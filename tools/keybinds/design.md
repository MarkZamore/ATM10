# Раскладка ATM10: как устроен `launcher/controls-preset.txt`

Пресет — полная раскладка: все 531 клавиша, которые регистрируют моды сборки, по одной строке в синтаксисе `options.txt`. Лаунчер по кнопке «Пресет настроек управления» записывает эти строки в `options.txt` инстанса; игра читает файл при старте. Скрипт `check_preset.py` доказывает, что ни одну пару экран «Управление» не покрасит красным.

Инструменты те же, что в LL8 Extended, и правила конфликтов те же — NeoForge 1.21.1, `KeyMapping.same`. Здесь записано только то, что своё: цифры этой сборки и решения, которые из них вышли.

## Откуда взяты цифры

Живой инстанс (`options.txt` после запуска сборки) регистрирует 530 клавиш; ещё одну — `key.jei.internal.enter.key` — достал из байткода `scan_keys.py`. Назначено было 328, из них **273 (83 %) стояли в 61 конфликтной группе**: V — 18 биндов, LShift — 14, C — 13, LCtrl — 12, LAlt и G — по 11, H — 10, B и R — по 8. Ни одна из этих групп не работала: экран «Управление» красил их красным, а в игре срабатывал кто первый попался.

Контексты `scan_keys.py` вынул из байткода для 47 биндов; остальным приписал `UNIVERSAL` — самое строгое предположение. Вышло `UNIVERSAL` 491, `IN_GAME` 26, `GUI` 14.

## Что решило раскладку

- **Одна клавиша — один бинд.** Разрешённых конфликтов в этой сборке нет вовсе: `allowed-conflicts.json` пуст. В LL8 их три, и все три — жест, который мод сам задал по умолчанию; в ATM10 таких дефолтов не нашлось, а выдумывать их не за чем.
- **Сочетаний с модификаторами нет.** Причина та же, что в LL8, и она про сам NeoForge: `KeyMappingLookup.getAll` при зажатом модификаторе отдаёт **только** бинд с этим модификатором, поэтому `Ctrl+E` не добавляется к `E`, а требует освободить `E`. Модификатор — это физическое состояние Shift/Ctrl/Alt, а не привязка: крадёшься на Shift — живут все `Shift+X`, бежишь на Ctrl — все `Ctrl+X` срабатывают от простого X. Сочетание не даёт места, а забирает.
- **Ваниль не тронута**, кроме трёх снятых: сохранение и загрузка панели (только творческий режим — освободили C и X) и «Социальные взаимодействия» (P). Снят и `options.narrator`: `Ctrl+B`, включающий диктора по случайности, — вся суть претензии к нему.
- **JEI держит R и U.** Это мышечная память любого, кто играл в модовую сборку, и её не за что ломать. Закладка JEI уехала с **A** на **Y**: A — это шаг влево, и оставить на нём `UNIVERSAL`-бинд означало красить экран красным против ходьбы.
- **Цифровой блок не используется** — не у всякой клавиатуры он есть. Пять клавиш Jade и обе клавиши Placebo, которые там сидели, сняты: настройки Jade открываются из списка модов, рецепты и применения показывает JEI, а «крылья и следы» Placebo — косметика.
- **Экранные бинды живут там, где сама игра клавишу не занимает**: LAlt (подробности Jade), `'` (сравнение снаряжения Apotheosis), Delete (TrashSlot), F1 (расширенные сведения о квесте). Имена всех четырёх собираются в рантайме, поэтому байткод о них не говорит ничего и `scan_keys.py` честно считает их `UNIVERSAL` — на этих клавишах это ничего не стоит.

## Раскладка

Рука на WASD; ближние буквы — самым частым действиям.

| Клавиша | Действие |
|---|---|
| **B** | Sophisticated Backpacks: открыть рюкзак |
| **C** | Ars Nouveau: книга заклинаний |
| **G** | Ender IO: посох путешествий |
| **H** | PneumaticCraft: взлом шлемом |
| **I** | Aether: инвентарь аксессуаров |
| **J** | Iron's Spellbooks: колесо заклинаний (удерживать) |
| **K** · **O** | Iris: шейдеры вкл/выкл · выбор шейдерпака |
| **M** | JourneyMap: карта мира |
| **N** | Occultism: доступ к хранилищу |
| **P** | Iron Jetpacks: парение |
| **R** · **U** | JEI: рецепт · применения |
| **V** | Iron's Spellbooks: применить заклинание |
| **X** · **Z** | Дракон (Ice and Fire): дыхание · вниз |
| **Y** | JEI: закладка |
| `` ` `` | FTB Ultimine |
| `\` | Mekanism: настройка модулей |
| `[` `]` | Sophisticated Core: в хранилище · в инвентарь |
| `;` | Дракон (Ice and Fire): удар |
| `'` | Apotheosis: сравнить снаряжение (в экранах) |
| `,` `.` | Iron Jetpacks: тяга − / + |
| `-` `=` | JourneyMap: отдалить / приблизить |
| `0` | Deeper Darker: ускорение элитры души |
| F4 | Draconic Evolution: настройка инструмента |
| F6 | Building Gadgets 2: настройки |
| F7 · F9 | More Overlays: свет · границы чанков |
| F8 | Iron Jetpacks: двигатель |
| F10 | Railcraft: режим локомотива |
| F12 | Utility Vest: радиальное меню |
| F1 | FTB Quests: расширенные сведения (в экранах) |
| Insert | Apotheosis: режим радиальной добычи |
| Home · End | Modern Industrialization: полёт · Mekanism: режим предмета |
| PgUp · PgDn | JEI: предыдущая / следующая страница рецептов |
| Enter | Structurize: поставить постройку |
| Backspace | Building Gadgets 2: отменить |
| ScrollLock · Pause | Cataclysm: способность · EvilCraft: возвышенный верстак |
| ↑ | FTB Ultimine: форма (цикл замкнут, одна клавиша достаёт все) |
| ↓ | Дракон (Ice and Fire): камера |
| ← → | Railcraft: локомотив медленнее / быстрее |
| Delete (в экранах) | TrashSlot |
| LAlt (удерживать) | Jade: подробности |
| RShift · RCtrl · RAlt | Create: модификаторы Shift/Ctrl/Alt |
| CapsLock · Menu | Create: ближайшие ящики · оверлей схематики |
| Мышь 4 · 5 | Relics: меню способностей · Neovitae: кровавый щит |

Свободны для личных биндов: PrintScreen, мышь 6–8 и всё, что снято, — снятая клавиша ни с чем не конфликтует, пока её не назначили.

## Что снято и почему

Целиком: Super Factory Manager (17 биндов — модификаторы, повторяющие Shift/Ctrl/Alt, и текстовый редактор с своими экранными клавишами), Mahou Tsukai (7), Crafting Tweaks (у экрана крафта есть кнопки), Structurize кроме «поставить» (постройка двигается мышью), Integrated Terminals и Dynamics, Modular Routers, SecurityCraft (клавиши режима камеры), Building Gadgets 2 кроме настроек и отмены, Easy Villagers, Crystalix, Silent Gear (три удерживаемые клавиши подсказок — то же есть в тултипах), Occultism кроме доступа к хранилищу (сатчел и эндер-сатчел открываются использованием предмета).

Точечно: у FTB Quests — весь редактор и навигация (в экране всё делается мышью); у JourneyMap — панорамирование, пресеты и настройки (доступны с карты); у FTB Chunks — карта и зум (карта открывается кнопкой на боковой панели инвентаря, а мир показывает JourneyMap); у Mekanism — описание, HUD и четыре режима брони (те же `ModuleConfig` переключает Module Tweaker на `\`); у Jade — пять клавиш цифрового блока; у Placebo — крылья и следы.

Каждая снятая строка в файле помнит прежнюю клавишу: `# unbound: was v`.

## Как править

1. Правьте `tools/keybinds/build_preset.py` — таблицу `LAYOUT`, — и пересоберите: `python tools/keybinds/build_preset.py`. Руками файл не правят: он полный, и одна строка из 531 теряется в нём слишком легко.
2. `python tools/keybinds/check_preset.py` — ноль проблем; `--report` печатает раскладку по клавишам.
3. После добавления или удаления модов: `python tools/keybinds/scan_keys.py --options <options.txt инстанса, запускавшего сборку>` — обновит `registered-keys.tsv` и `contexts.json`. Инстанс, к которому уже применяли пресет, снимком не считается: он вернёт сам пресет.
4. Коммит и push — `publish.yml` прогоняет checker и публикует пак; у игроков кнопка в лаунчере разблокируется и предлагает применить новую версию.
